	// {endpoint}::{mode}
	//   rationale: {rationale}
	{{
		t0 := time.Now()
		v, e := c.{go_method_name}({args})
		records = classify({endpoint:?}, {mode:?}, {min_tier:?}, {rationale:?}, v, e, time.Since(t0), &pass, &skip, &fail, records)
	}}
