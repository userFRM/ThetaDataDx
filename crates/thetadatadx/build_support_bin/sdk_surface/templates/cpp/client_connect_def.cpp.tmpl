MddsClient MddsClient::connect(const Credentials& creds, const Config& config) {
    auto h = tdx_mdds_client_connect(creds.get(), config.get());
    if (!h) detail::throw_last_ffi_error();
    return MddsClient(h);
}
