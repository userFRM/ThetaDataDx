/// Dispatch a validated endpoint call into the generated SDK adapter set.
///
/// The build script emits one match arm per endpoint from the shared endpoint
/// metadata so registry-driven projections stay aligned with the SDK surface.
pub async fn invoke_generated_endpoint(
    client: &crate::mdds::MddsClient,
    name: &str,
    args: &EndpointArgs,
) -> Result<EndpointOutput, EndpointError> {
    match name {
