---
title: Price
description: "Get the latest price snapshot for one or more indices."
aside: false
---

<!-- @generated by `generate_docs_site` from endpoint_surface.toml + tick_schema.toml. Do not edit by hand. -->

<script setup>
const cfg = {
  httpPath: "v3/index/snapshot/price",
  method: { rust: "index_snapshot_price", python: "index_snapshot_price", ts: "indexSnapshotPrice", cpp: "index_snapshot_price" },
  required: [{ key: "symbol", type: "symbols", default: "SPX" }],
  optional: [{ key: "min_time", type: "string", default: "" }],
  print: ["date", "ms_of_day", "price"],
  returns: "PriceTick",
  sample: [
    {"price":6395.78,"symbol":"SPX","timestamp":"2025-08-20T16:02:06"},
  ],
}
</script>

# Price

<TierBadge tier="standard" />

Get the latest price snapshot for one or more indices.

- Retrieves a real-time last index price.
- Exchanges typically generate a price report every second for popular indices like SPX.

<RequestBuilder :cfg="cfg" />

## Parameters

| Name | Type | Required | Default | Description |
|---|---|---|---|---|
| `symbol` | symbols | yes | — | Comma-separated ticker symbols (e.g. AAPL,MSFT) |
| `min_time` | string | no | — | Minimum time filter |
| `timeout_ms` | int | no | — | Per-request deadline in milliseconds. 0 means no deadline. |

## Response

Rows of `PriceTick`:

| Field | Type | Description |
|---|---|---|
| `ms_of_day` | i32 | Milliseconds since midnight Eastern Time. |
| `price` | f64 | Index value. |
| `date` | i32 | Trading date as a YYYYMMDD integer. |

