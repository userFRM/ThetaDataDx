---
title: Symbols
description: "List all available stock ticker symbols."
---

<!-- @generated by `generate_docs_site` from endpoint_surface.toml + tick_schema.toml. Do not edit by hand. -->

# Symbols

<TierBadge tier="free" />

List all available stock ticker symbols.

A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for stocks. This endpoint is updated overnight.

<SdkTabs>

<template #rust>

```rust
pub fn stock_list_symbols(&self) -> StockListSymbolsBuilder<'_>
```

Execute with `.await` → `Result<Vec<String>, Error>`.

**Example**

```rust
let rows = tdx.stock_list_symbols().await?;
for value in &rows {
    println!("{value}");
}
```

</template>

<template #python>

```python
Client.stock_list_symbols(*, timeout_ms=None) -> StringList
```

`stock_list_symbols_async(...)` awaits the same call shape.

**Example**

```python
rows = tdx.stock_list_symbols()
for value in rows:
    print(value)
```

</template>

<template #typescript>

```typescript
stockListSymbols(options?: { ... }): Promise<Array<string>>
```

Optional parameters ride in a single trailing options object: `timeoutMs?: number`.

**Example**

```typescript
const rows = await tdx.stockListSymbols();
for (const value of rows) {
  console.log(value);
}
```

</template>

<template #cpp>

```cpp
std::vector<std::string> stock_list_symbols(
    const EndpointRequestOptions& options = {}) const;
```

Throws `thetadatadx::Error` on failure.

**Example**

```cpp
auto rows = client.stock_list_symbols();
for (const auto& value : rows) {
    std::cout << value << "\n";
}
```

</template>

<template #http>

```http
GET http://127.0.0.1:25503/v3/stock/list/symbols
```

Served by the bundled [server binary](/server/); responses stream as JSON, CSV, or NDJSON via the `format` parameter.

**Example**

```bash
curl 'http://127.0.0.1:25503/v3/stock/list/symbols'
```

</template>

</SdkTabs>

## Parameters

| Name | Type | Required | Default | Description |
|---|---|---|---|---|
| `timeout_ms` | int | no | — | Per-request deadline in milliseconds. 0 means no deadline. |

## Response

A list of strings — one `symbol` value per row.

