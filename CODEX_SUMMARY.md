# Codex Summary

| Finding | Status | Commit | Notes |
|---|---|---|---|
| C1 | fixed | bb677e94 | Confirmed napi-rs holds the `aborted` read lock across blocking TSFN calls; release now wakes before taking the write lock. Checks: `cargo test --manifest-path thetadatadx-ts/Cargo.toml teardown_deadlock_tests --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H1 | fixed | 97fa2280 | Confirmed decoded in-session `Disconnected { reason }` was published but did not break the inner loop. The read loop now carries reconnectable decoded reasons into the reconnect classifier. Checks: `cargo test --manifest-path thetadatadx-rs/Cargo.toml decoded_disconnect_reason_reaches_reconnect_classifier --lib`; `cargo build --manifest-path thetadatadx-rs/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-rs/Cargo.toml --all-targets --features __internal -- -D warnings`. |
| H2 | fixed | 7ec740fd | Confirmed reconnect could reuse a callback whose TSFN had been closed by teardown and that ignored `Status::Closing` silently dropped delivery. Reconnect now rejects closed callbacks and unexpected callback closure marks the stream failed. Checks: `cargo run -p thetadatadx --features config-file --bin generate_sdk_surfaces -- --check`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml callback_delivery_failure_tests --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H3 | fixed | 404d2979 | Confirmed standalone reconnect reused the same callback pointer for overlapping starts, making pointer ownership insufficient. Each standalone start now gets a generation token, and reconnect restores subscriptions only while its generation still owns the session. Checks: `cargo test --manifest-path thetadatadx-ts/Cargo.toml callback_generation_tests --lib`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H4 | fixed | 958075e4 | Confirmed generated TS historical stream methods used fire-and-forget TSFN calls, so JS handler throws were not observed by Rust. The generator now emits per-chunk awaited `call_async_catch` delivery, records the first handler error, skips later callbacks, and rejects the returned Promise. Checks: `cargo run -p thetadatadx --features config-file --bin generate_sdk_surfaces -- --check`; `cargo test --manifest-path thetadatadx-rs/Cargo.toml --features config-file --bin generate_sdk_surfaces`; `cargo clippy --manifest-path thetadatadx-rs/Cargo.toml --features config-file --bin generate_sdk_surfaces -- -D warnings`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H5 | fixed | de0c807f | Confirmed `Client::close()` had the drain-timeout reclaimer path but the unified C++ client had no destructor calling it. `~Client()` now routes RAII destruction through `close()`. Checks: `cargo build -p thetadatadx-ffi`; `cmake -S thetadatadx-cpp -B target/cpp-h5 -DTHETADATADX_FFI_DIR=$PWD/target/debug -DTHETADATADX_CPP_BUILD_TESTS=OFF`; `cmake --build target/cpp-h5 --target thetadatadx_cpp -j2`. |
| H6 | fixed | 9b9c26bd | Confirmed the C ABI documented `_free` as safe during a pull even though entry points must borrow the opaque handle box before cloning the inner `Arc`. The contract now requires `_free` serialization, preserves `_close` as the cross-thread wake, and the C++ wrapper pins a `shared_ptr` during pulls. Checks: `cargo fmt --manifest-path thetadatadx-ffi/Cargo.toml`; `cargo test -p thetadatadx-ffi streaming_batches --lib`; `cargo build -p thetadatadx-ffi`; `cargo clippy -p thetadatadx-ffi --all-targets -- -D warnings`; `cmake --build target/cpp-h5 --target thetadatadx_cpp -j2`. |
| H7 | fixed | a283f3db | Calendar `WireColumns` now mirrors the v3 parser header table: `type` projects both `is_open` and `status`, `open`/`close` project session times, and `date` remains present only when the wire sends it. Checks: `cargo fmt --manifest-path thetadatadx-rs/Cargo.toml`; `cargo test -p thetadatadx present_calendar_day --lib`; `cargo test -p thetadatadx --features "arrow,polars,__internal" --test test_column_projection calendar`; `cargo build -p thetadatadx --features "arrow,polars,__internal"`; `cargo clippy -p thetadatadx --all-targets --features "arrow,polars,__internal" -- -D warnings`. |
| H8 | fixed | 550671be | `ThetaDataDxColumnPresence` now carries the constant `symbol` broadcast in addition to per-row symbols, reconstructs it for projected serializers, and keeps per-row symbols authoritative. Checks: `cargo fmt --manifest-path thetadatadx-ffi/Cargo.toml`; `cargo test -p thetadatadx-ffi column_presence_carrier_tests --lib`; `cargo test -p thetadatadx-ffi --test arrow_ipc_projected`; `cargo build -p thetadatadx-ffi`; `cargo clippy -p thetadatadx-ffi --all-targets -- -D warnings`; `cmake --build target/cpp-h5 --target thetadatadx_cpp -j2`. |
| H9 | fixed | e27dcbd9 | Python snapshot methods with `Symbols` inputs now return typed list wrappers instead of plain `PyList`, preserving `ColumnPresence::symbols()` for per-row attribution and DataFrame projection. Checks: `cargo run -p thetadatadx --features config-file --bin generate_sdk_surfaces -- --check`; `cargo test --manifest-path thetadatadx-rs/Cargo.toml --features config-file --bin generate_sdk_surfaces`; `cargo clippy --manifest-path thetadatadx-rs/Cargo.toml --features config-file --bin generate_sdk_surfaces -- -D warnings`; `cargo build --manifest-path thetadatadx-py/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-py/Cargo.toml --all-targets -- -D warnings`; `LD_LIBRARY_PATH=/home/linuxbrew/.linuxbrew/lib:/home/linuxbrew/.linuxbrew/opt/python@3.14/lib cargo test --manifest-path thetadatadx-py/Cargo.toml`. |
| H10 | fixed | f3e7f84c | TypeScript npm publish now runs only for push events on version tags; manual dispatch remains build-only. Checks: `git diff --check -- .github/workflows/typescript.yml`; parsed `.github/workflows/typescript.yml` with PyYAML and asserted `jobs.publish.if == "github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')"`. |
| H11 | fixed | 03153c00 | The release validator now captures `check_agreement.py`'s exit status before printing its output, so agreement failures add a release-blocking failure. Checks: `bash -n scripts/release/validate_release.sh`; `python3 scripts/release/tests/test_validate_release.py`; `git diff --check -- scripts/release/validate_release.sh scripts/release/tests/test_validate_release.py`. |
| H12 | fixed | 03153c00 | Python and C++ validator subprocesses now fail closed when they exit nonzero or do not emit `COUNTS:p:s:f`, instead of treating empty counts as zero failures. Checks: `bash -n scripts/release/validate_release.sh`; `python3 scripts/release/tests/test_validate_release.py`; `git diff --check -- scripts/release/validate_release.sh scripts/release/tests/test_validate_release.py`. |
| H13 | fixed | 4b4cbba2 | Verified `thetadatadx-rs/src/fpss/pinning.rs` enforces an FPSS hostname allowlist, leaf `SubjectPublicKeyInfo` SHA-256 pin, and TLS handshake signature verification. `SECURITY.md` now describes that behavior instead of saying streaming skips certificate verification. Checks: `cargo test -p thetadatadx pinning --lib`; `python3 scripts/ci/check_client_facing_vocab.py`; `python3 scripts/ci/check_no_re_framing.py`; `git diff --check -- SECURITY.md`. |
| H14 | fixed | 613b1684 | Removed the obsolete AI docs claim that the MCP server exposes offline Greeks tools; the offline MCP surface is `ping` only. Checks: `git diff --check -- docs-site/docs/articles/ai-llms.md`; `python3 scripts/ci/check_docs_consistency.py`; `python3 scripts/ci/check_no_re_framing.py`; `python3 scripts/ci/check_client_facing_vocab.py`; `python3 scripts/ci/check_public_surface_leak.py`. Docs-site build not run because `docs-site/node_modules` is absent and the offline rule prevents installing dependencies. |
| H15 | fixed | c8a0463f | Verified `tools/server/src/main.rs` defaults `--bind` to `0.0.0.0`; server docs now state the all-interfaces default and recommend `--bind 127.0.0.1` for loopback-only exposure. Checks: `git diff --check -- docs-site/docs/server/index.md`; `python3 scripts/ci/check_docs_consistency.py`; `python3 scripts/ci/check_no_re_framing.py`; `python3 scripts/ci/check_client_facing_vocab.py`; `python3 scripts/ci/check_public_surface_leak.py`. Docs-site build not run because `docs-site/node_modules` is absent and the offline rule prevents installing dependencies. |
| M1 | fixed | a2c39231 | Standalone TypeScript `reconnect()` now moves old-session stop/drain teardown onto a blocking worker before opening the fresh session, matching the nonblocking connect/restore discipline. Checks: `cargo fmt --manifest-path thetadatadx-ts/Cargo.toml`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml callback_generation_tests --lib`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml teardown_deadlock_tests --lib`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`; `git diff --check -- thetadatadx-ts/src/fpss_client.rs`. |
