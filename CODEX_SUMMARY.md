# Codex Summary

| Finding | Status | Commit | Notes |
|---|---|---|---|
| C1 | fixed | bb677e94 | Confirmed napi-rs holds the `aborted` read lock across blocking TSFN calls; release now wakes before taking the write lock. Checks: `cargo test --manifest-path thetadatadx-ts/Cargo.toml teardown_deadlock_tests --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H1 | fixed | 97fa2280 | Confirmed decoded in-session `Disconnected { reason }` was published but did not break the inner loop. The read loop now carries reconnectable decoded reasons into the reconnect classifier. Checks: `cargo test --manifest-path thetadatadx-rs/Cargo.toml decoded_disconnect_reason_reaches_reconnect_classifier --lib`; `cargo build --manifest-path thetadatadx-rs/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-rs/Cargo.toml --all-targets --features __internal -- -D warnings`. |
| H2 | fixed | 7ec740fd | Confirmed reconnect could reuse a callback whose TSFN had been closed by teardown and that ignored `Status::Closing` silently dropped delivery. Reconnect now rejects closed callbacks and unexpected callback closure marks the stream failed. Checks: `cargo run -p thetadatadx --features config-file --bin generate_sdk_surfaces -- --check`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml callback_delivery_failure_tests --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H3 | fixed | 404d2979 | Confirmed standalone reconnect reused the same callback pointer for overlapping starts, making pointer ownership insufficient. Each standalone start now gets a generation token, and reconnect restores subscriptions only while its generation still owns the session. Checks: `cargo test --manifest-path thetadatadx-ts/Cargo.toml callback_generation_tests --lib`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
| H4 | fixed | 958075e4 | Confirmed generated TS historical stream methods used fire-and-forget TSFN calls, so JS handler throws were not observed by Rust. The generator now emits per-chunk awaited `call_async_catch` delivery, records the first handler error, skips later callbacks, and rejects the returned Promise. Checks: `cargo run -p thetadatadx --features config-file --bin generate_sdk_surfaces -- --check`; `cargo test --manifest-path thetadatadx-rs/Cargo.toml --features config-file --bin generate_sdk_surfaces`; `cargo clippy --manifest-path thetadatadx-rs/Cargo.toml --features config-file --bin generate_sdk_surfaces -- -D warnings`; `cargo test --manifest-path thetadatadx-ts/Cargo.toml --lib`; `cargo build --manifest-path thetadatadx-ts/Cargo.toml`; `cargo clippy --manifest-path thetadatadx-ts/Cargo.toml --all-targets -- -D warnings`. |
