// Code generated by build.rs from tick_schema.toml; DO NOT EDIT.

package thetadatadx

/*
#include "ffi_bridge.h"
*/
import "C"

import "unsafe"

func convertCalendarDays(arr C.TdxTickArray) []CalendarDay {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cCalendarDay)(arr.data), n)
    result := make([]CalendarDay, n)
    for i, t := range src {
        result[i] = CalendarDay{
            Date: int(t.Date),
            IsOpen: t.IsOpen != 0,
            OpenTime: int(t.OpenTime),
            CloseTime: int(t.CloseTime),
            Status: int(t.Status),
        }
    }
    return result
}

func convertEodTicks(arr C.TdxTickArray) []EodTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cEodTick)(arr.data), n)
    result := make([]EodTick, n)
    for i, t := range src {
        result[i] = EodTick{
            MsOfDay: int(t.MsOfDay),
            MsOfDay2: int(t.MsOfDay2),
            Open: t.Open,
            High: t.High,
            Low: t.Low,
            Close: t.Close,
            Volume: t.Volume,
            Count: t.Count,
            BidSize: int(t.BidSize),
            BidExchange: int(t.BidExchange),
            Bid: t.Bid,
            BidCondition: int(t.BidCondition),
            AskSize: int(t.AskSize),
            AskExchange: int(t.AskExchange),
            Ask: t.Ask,
            AskCondition: int(t.AskCondition),
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertGreeksTicks(arr C.TdxTickArray) []GreeksTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cGreeksTick)(arr.data), n)
    result := make([]GreeksTick, n)
    for i, t := range src {
        result[i] = GreeksTick{
            MsOfDay: int(t.MsOfDay),
            IV: t.ImpliedVolatility,
            Delta: t.Delta,
            Gamma: t.Gamma,
            Theta: t.Theta,
            Vega: t.Vega,
            Rho: t.Rho,
            IVError: t.IvError,
            Vanna: t.Vanna,
            Charm: t.Charm,
            Vomma: t.Vomma,
            Veta: t.Veta,
            Speed: t.Speed,
            Zomma: t.Zomma,
            Color: t.Color,
            Ultima: t.Ultima,
            D1: t.D1,
            D2: t.D2,
            DualDelta: t.DualDelta,
            DualGamma: t.DualGamma,
            Epsilon: t.Epsilon,
            Lambda: t.Lambda,
            Vera: t.Vera,
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertInterestRateTicks(arr C.TdxTickArray) []InterestRateTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cInterestRateTick)(arr.data), n)
    result := make([]InterestRateTick, n)
    for i, t := range src {
        result[i] = InterestRateTick{
            MsOfDay: int(t.MsOfDay),
            Rate: t.Rate,
            Date: int(t.Date),
        }
    }
    return result
}

func convertIvTicks(arr C.TdxTickArray) []IVTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cIvTick)(arr.data), n)
    result := make([]IVTick, n)
    for i, t := range src {
        result[i] = IVTick{
            MsOfDay: int(t.MsOfDay),
            IV: t.ImpliedVolatility,
            IVError: t.IvError,
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertMarketValueTicks(arr C.TdxTickArray) []MarketValueTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cMarketValueTick)(arr.data), n)
    result := make([]MarketValueTick, n)
    for i, t := range src {
        result[i] = MarketValueTick{
            MsOfDay: int(t.MsOfDay),
            MarketBid: t.MarketBid,
            MarketAsk: t.MarketAsk,
            MarketPrice: t.MarketPrice,
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertOhlcTicks(arr C.TdxTickArray) []OhlcTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cOhlcTick)(arr.data), n)
    result := make([]OhlcTick, n)
    for i, t := range src {
        result[i] = OhlcTick{
            MsOfDay: int(t.MsOfDay),
            Open: t.Open,
            High: t.High,
            Low: t.Low,
            Close: t.Close,
            Volume: t.Volume,
            Count: t.Count,
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertOpenInterestTicks(arr C.TdxTickArray) []OpenInterestTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cOpenInterestTick)(arr.data), n)
    result := make([]OpenInterestTick, n)
    for i, t := range src {
        result[i] = OpenInterestTick{
            MsOfDay: int(t.MsOfDay),
            OpenInterest: int(t.OpenInterest),
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertOptionContracts(arr C.TdxOptionContractArray) []OptionContract {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cOptionContract)(arr.data), n)
    result := make([]OptionContract, n)
    for i, t := range src {
        root := ""
        if t.Root != 0 {
            root = C.GoString((*C.char)(unsafe.Pointer(t.Root)))
        }
        result[i] = OptionContract{
            Root: root,
            Expiration: int(t.Expiration),
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertPriceTicks(arr C.TdxTickArray) []PriceTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cPriceTick)(arr.data), n)
    result := make([]PriceTick, n)
    for i, t := range src {
        result[i] = PriceTick{
            MsOfDay: int(t.MsOfDay),
            Price: t.Price,
            Date: int(t.Date),
        }
    }
    return result
}

func convertQuoteTicks(arr C.TdxTickArray) []QuoteTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cQuoteTick)(arr.data), n)
    result := make([]QuoteTick, n)
    for i, t := range src {
        result[i] = QuoteTick{
            MsOfDay: int(t.MsOfDay),
            BidSize: int(t.BidSize),
            BidExchange: int(t.BidExchange),
            Bid: t.Bid,
            BidCondition: int(t.BidCondition),
            AskSize: int(t.AskSize),
            AskExchange: int(t.AskExchange),
            Ask: t.Ask,
            AskCondition: int(t.AskCondition),
            Date: int(t.Date),
            Midpoint: t.Midpoint,
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertTradeQuoteTicks(arr C.TdxTickArray) []TradeQuoteTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cTradeQuoteTick)(arr.data), n)
    result := make([]TradeQuoteTick, n)
    for i, t := range src {
        result[i] = TradeQuoteTick{
            MsOfDay: int(t.MsOfDay),
            Sequence: int(t.Sequence),
            ExtCondition1: int(t.ExtCondition1),
            ExtCondition2: int(t.ExtCondition2),
            ExtCondition3: int(t.ExtCondition3),
            ExtCondition4: int(t.ExtCondition4),
            Condition: int(t.Condition),
            Size: int(t.Size),
            Exchange: int(t.Exchange),
            Price: t.Price,
            ConditionFlags: int(t.ConditionFlags),
            PriceFlags: int(t.PriceFlags),
            VolumeType: int(t.VolumeType),
            RecordsBack: int(t.RecordsBack),
            QuoteMsOfDay: int(t.QuoteMsOfDay),
            BidSize: int(t.BidSize),
            BidExchange: int(t.BidExchange),
            Bid: t.Bid,
            BidCondition: int(t.BidCondition),
            AskSize: int(t.AskSize),
            AskExchange: int(t.AskExchange),
            Ask: t.Ask,
            AskCondition: int(t.AskCondition),
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

func convertTradeTicks(arr C.TdxTickArray) []TradeTick {
    if arr.data == nil || arr.len == 0 {
        return nil
    }
    n := int(arr.len)
    src := unsafe.Slice((*cTradeTick)(arr.data), n)
    result := make([]TradeTick, n)
    for i, t := range src {
        result[i] = TradeTick{
            MsOfDay: int(t.MsOfDay),
            Sequence: int(t.Sequence),
            ExtCondition1: int(t.ExtCondition1),
            ExtCondition2: int(t.ExtCondition2),
            ExtCondition3: int(t.ExtCondition3),
            ExtCondition4: int(t.ExtCondition4),
            Condition: int(t.Condition),
            Size: int(t.Size),
            Exchange: int(t.Exchange),
            Price: t.Price,
            ConditionFlags: int(t.ConditionFlags),
            PriceFlags: int(t.PriceFlags),
            VolumeType: int(t.VolumeType),
            RecordsBack: int(t.RecordsBack),
            Date: int(t.Date),
            Expiration: t.Expiration,
            Strike: t.Strike,
            Right: RightStr(t.Right),
        }
    }
    return result
}

