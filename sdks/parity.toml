# Cross-binding parity matrix (Gate 2 / issue #545).
#
# Every user-facing class registered on Python is declared here with
# its current state in TypeScript (napi) and C++ (`thetadx.hpp`).
# `scripts/check_binding_parity.py` parses this file and compares the
# actual binding state of each SDK to the declared state; mismatches
# fail CI.
#
# When you intentionally ship a binding asymmetry (e.g. you land a
# Python pyclass first and TS/C++ are tracked separately), update the
# row here. When you intend to keep the three bindings in lockstep,
# every row should be `true` in every column and CI will block any
# Python-only PR that forgets to update TS / C++.

[[class]]
name = "Credentials"
python = true
typescript = false  # JS takes credentials as constructor args, not a class
cpp = true

[[class]]
name = "Config"
python = true
typescript = true   # exposed as `Config` napi class with `production() / dev() / stage()` factories
cpp = true


[[class]]
name = "ThetaDataDxClient"
python = true
typescript = true
cpp = true          # exposed as `tdx::Client` + `tdx::UnifiedClient`

[[class]]
name = "AsyncThetaDataDxClient"
python = true
typescript = false  # not yet ported — issue #545 follow-up
cpp = false         # not yet ported — issue #545 follow-up

[[class]]
name = "FpssClient"
python = true
typescript = false  # not yet ported — issue #545 follow-up
cpp = true

[[class]]
name = "MddsClient"
python = true
typescript = false  # not yet ported — issue #545 follow-up
cpp = false         # not yet ported — issue #545 follow-up

[[class]]
name = "StreamingSession"
python = true
typescript = false  # JS uses a Promise-based async iterator instead — equivalent ergonomics
cpp = false         # C++ uses RAII guard pattern instead

[[class]]
name = "Contract"
python = true
typescript = true   # exposed as `ContractRef` (Contract aliased post-build)
cpp = true          # exposed as `tdx::FluentContract`

[[class]]
name = "ContractRef"
python = true
typescript = true   # primary class name on TS side (Contract is the post-build alias)
cpp = false         # C++ uses the C ABI `TdxContract` struct; no dedicated C++ class wrapper

[[class]]
name = "CalendarDay"
python = true
typescript = true
cpp = true

[[class]]
name = "NoDataFoundError"
python = true
typescript = false
cpp = false         # C++ folds NoData into the generic `Error` class with `.kind == NoData`

[[class]]
name = "TimeoutError"
python = true
typescript = false
cpp = false         # C++ folds Timeout into the generic `Error` class with `.kind == Timeout`

[[class]]
name = "Subscription"
python = true
typescript = true
cpp = true          # exposed as `tdx::FluentSubscription`

[[class]]
name = "SecType"
python = true
typescript = true
cpp = true          # exposed as `tdx::FluentSecType`

[[class]]
name = "FlatFilesNamespace"
python = true
typescript = true
cpp = true          # exposed as `tdx::FlatFiles`

[[class]]
name = "FlatFileRowList"
python = true
typescript = true
cpp = true

# ── Typed exception hierarchy.
#
# C++ exposes one class per leaf, matching the Python tree.
# TypeScript surfaces every error variant via a single discriminated
# `Error` class with `.kind` (see `index.d.ts` line ~1127); the
# narrowing pattern is `if (e.kind === "InvalidCredentials")`. That
# is intentional — JS error classes don't compose with `instanceof`
# across native module boundaries — so each row below records the TS
# column as `false` rather than misrepresenting the surface.

[[class]]
name = "ThetaDataError"
python = true
typescript = false
cpp = true

[[class]]
name = "AuthenticationError"
python = true
typescript = false
cpp = true

[[class]]
name = "InvalidCredentialsError"
python = true
typescript = false
cpp = true

[[class]]
name = "SubscriptionError"
python = true
typescript = false
cpp = true

[[class]]
name = "RateLimitError"
python = true
typescript = false
cpp = true

[[class]]
name = "NetworkError"
python = true
typescript = false
cpp = true

[[class]]
name = "SchemaMismatchError"
python = true
typescript = false
cpp = true

[[class]]
name = "StreamError"
python = true
typescript = false
cpp = true

# ─── FlatFilesConfig cross-binding setters ─────────────────────────────
#
# Per-field access on the legacy `FlatFilesConfig` sub-config. Bound on
# every binding: Python via `Config.flatfiles_*` setters/getters,
# TypeScript via `Config.setFlatFiles*`, C++ via
# `tdx::Config::set_flatfiles_*` (which forwards to the FFI
# `tdx_config_set_flatfiles_*` symbols). `max_attempts` is `u32`, the
# two `Duration` fields cross the binding boundary as `u64` seconds
# (`initial_backoff_secs` / `max_backoff_secs`).

[[class]]
name = "FlatFilesConfig.max_attempts"
python = true
typescript = true
cpp = true

[[class]]
name = "FlatFilesConfig.initial_backoff_secs"
python = true
typescript = true
cpp = true

[[class]]
name = "FlatFilesConfig.max_backoff_secs"
python = true
typescript = true
cpp = true

[[class]]
name = "FlatFilesConfig.jitter"
python = true
typescript = true
cpp = true

# ─── MddsConfig cross-binding setters ──────────────────────────────────
#
# Tuning knobs on the MDDS sub-config exposed across every binding.
# `concurrent_requests` / `warn_on_buffered_threshold_bytes` are bound
# via `Config.<name>` (Python), `Config.set<Name>` (TypeScript napi),
# `tdx::Config::set_<name>` (C++ forwarder over the C ABI).

[[class]]
name = "MddsConfig.concurrent_requests"
python = true
typescript = true
cpp = true

[[class]]
name = "MddsConfig.warn_on_buffered_threshold_bytes"
python = true
typescript = true
cpp = true

# ─── ReconnectConfig cross-binding setters ─────────────────────────────
#
# FPSS reconnect policy + per-class attempt budgets + stable-window
# timer. Bound on every binding: Python via `Config.reconnect_*`
# setters, TypeScript via `Config.setReconnect*`, C++ via
# `tdx::Config::set_reconnect_*` (which forwards to the FFI
# `tdx_config_set_reconnect_*` symbols). Dotted-name rows track
# field-level parity; the parity script's class-existence check skips
# dotted rows pending the per-field granularity tightening (issue
# referenced in `scripts/check_binding_parity.py`).

[[class]]
name = "ReconnectConfig.policy"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.max_attempts"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.max_rate_limited_attempts"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.stable_window_secs"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.wait_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.wait_rate_limited_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.wait_max_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.wait_server_restart_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.jitter"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.max_elapsed_secs"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.max_server_restart_attempts"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.replay_burst_size"
python = true
typescript = true
cpp = true

[[class]]
name = "ReconnectConfig.replay_pace_ms"
python = true
typescript = true
cpp = true

# ─── RuntimeConfig cross-binding setters ───────────────────────────────
#
# Async-runtime sub-config knobs honoured by embedded bindings that own
# their tokio runtime (FFI / Python / napi). The
# `tokio_worker_threads` field uses the widened `(has_value, n)` ABI
# shape on the C surface so the `Some(0)` sentinel survives the C
# boundary. `RuntimeConfig::build_runtime` interprets the field for
# callers building their own runtime.

[[class]]
name = "RuntimeConfig.tokio_worker_threads"
python = true
typescript = true
cpp = true

# ─── RetryPolicy cross-binding setters ─────────────────────────────────
#
# Per-field access on `DirectConfig.retry`. The `delay_for_attempt`
# / `capped_backoff` / `disabled()` helpers stay Rust-only — they are
# method-shape helpers that callers can recompute from the four field
# values if needed. Field setters bound on every binding (Python via
# `Config.retry_*`, TS via `Config.setRetry*`, C++ via
# `tdx::Config::set_retry_*`); the dotted rows below track field-level
# parity and the parity script's class-existence check skips dotted
# rows pending the per-field granularity tightening (issue referenced
# in `scripts/check_binding_parity.py`).

[[class]]
name = "RetryPolicy.initial_delay_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "RetryPolicy.max_delay_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "RetryPolicy.max_attempts"
python = true
typescript = true
cpp = true

[[class]]
name = "RetryPolicy.jitter"
python = true
typescript = true
cpp = true

[[class]]
name = "RetryPolicy.max_elapsed_secs"
python = true
typescript = true
cpp = true

# ─── Cross-binding error-class name divergences ───────────────────────
#
# The Rust `Error` enum surfaces through different binding-language
# idioms (no per-language "rename for cosmetics" pass):
#
#   * Rust:       `thetadatadx::Error` (single error enum).
#   * Python:     runtime class is `Error` (matches the Rust name);
#                 the `.pyi` stub shadows it as `FpssParseError`
#                 (suffix-disambiguated against the generic
#                 `Error(ThetaDataError)` exception class) to avoid the
#                 mypy duplicate-class collision. Runtime callers still
#                 catch `thetadatadx.Error`.
#   * TypeScript: napi exposes the variants as a discriminated union
#                 over the standard JS `Error` superclass — `class
#                 Error extends globalThis.Error` is the napi default.
#   * C++:        FFI surface returns `int32_t` status codes per the
#                 `tdx_last_error()` thread-local string convention; the
#                 RAII wrapper exposes them as `tdx::FpssError` to
#                 disambiguate from `std::exception`.
#
# This row is informational — the parity script's class-existence
# check skips dotted rows.

[[class]]
name = "Error.cross_binding_name_divergence"
python = true
typescript = true
cpp = true

# ─── EOD-Greek + index-price tick anchors ─────────────────────────────
#
# Two tick types model the verified-live wire shape on two endpoints
# whose earlier routing dropped server-emitted columns:
#
#   * `GreeksEodTick` (39 columns) -- `option_history_greeks_eod`.
#     Previously routed to the bare `GreeksAllTick` (28 columns),
#     losing the twelve EOD trade/quote columns (`open`, `high`,
#     `low`, `close`, `volume`, `count`, `bid_size`, `bid_exchange`,
#     `bid_condition`, `ask_size`, `ask_exchange`, `ask_condition`).
#   * `IndexPriceAtTimeTick` (10 columns) -- `index_at_time_price`.
#     Previously routed to the bare `PriceTick` (3 columns), losing
#     the seven trade-side execution columns (`sequence`,
#     `ext_condition1..4`, `condition`, `size`, `exchange`) including
#     the SIP-exchange attribution field.
#
# Both ticks are implicitly tracked by the parity script's `*Tick`
# catch-all (`scripts/check_binding_parity.py::_is_implicitly_tracked`)
# alongside every other generator-emitted tick wrapper. The rows
# below are documentation anchors so the parity matrix records the
# Additions; the script does not gate on dotted-name rows.

[[class]]
name = "GreeksEodTick.cross_binding_anchor"
python = true       # `thetadatadx.GreeksEodTick` pyclass + `GreeksEodTickList` + Arrow + columnar
typescript = true   # `GreeksEodTick` napi class
cpp = true          # `tdx::GreeksEodTick` alias over `TdxGreeksEodTick`

[[class]]
name = "IndexPriceAtTimeTick.cross_binding_anchor"
python = true       # `thetadatadx.IndexPriceAtTimeTick` pyclass + `IndexPriceAtTimeTickList` + Arrow + columnar
typescript = true   # `IndexPriceAtTimeTick` napi class
cpp = true          # `tdx::IndexPriceAtTimeTick` alias over `TdxIndexPriceAtTimeTick`

# ─── Rust-only MDDS / FPSS knobs (Gate 2 tightening / #595) ─────────────
#
# Pub fields on `MddsConfig` / `FpssConfig` that the binding sweep has
# not yet exposed on Python / TypeScript / C++. These are deliberately
# Rust-only today — embedders that own the runtime can mutate the
# field directly through the `DirectConfig` struct, but the
# higher-level bindings do not expose a setter. The
# `rust_only = true` flag + `issue = "#595"` citation discloses the
# tracked-work status: #595 (the Gate 2 per-field granularity
# tightening) is the umbrella issue under which the cross-binding
# sweep for these fields would be planned.
#
# Two `MddsConfig` test-only setters (`mdds_host` / `mdds_port`) are
# exposed on Python alone for structural-test usage (point the
# historical channel at a refused endpoint to prove the streaming
# surface never opens it). The dotted rows below capture the
# partial-binding state via the `setter = "..."` override on Python.

[[class]]
name = "MddsConfig.host"
python = true       # `Config.mdds_host` test-only setter
typescript = false
cpp = false
setter = "mdds_host"

[[class]]
name = "MddsConfig.port"
python = true       # `Config.mdds_port` test-only setter
typescript = false
cpp = false
setter = "mdds_port"

[[class]]
name = "MddsConfig.tls"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.max_message_size"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.keepalive_secs"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.keepalive_timeout_secs"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.window_size_kb"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.connection_window_size_kb"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.connect_timeout_secs"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "MddsConfig.override_tier_clamp"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

[[class]]
name = "FpssConfig.hosts"
python = false
typescript = false
cpp = false
rust_only = true
issue = "#595"

# FPSS transport knobs are bound on every binding under `fpss_`-prefixed
# canonical names (Python `Config.fpss_*`, TypeScript `Config.setFpss*`,
# C++ `set_fpss_*`, FFI `tdx_config_set_fpss_*`). The prefix
# disambiguates the generic field names (`timeout_ms`, `ring_size`)
# against sibling sub-configs at the binding surface.

[[class]]
name = "FpssConfig.fpss_timeout_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_ring_size"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_ping_interval_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_connect_timeout_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_io_read_slice_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_data_watchdog_ms"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_keepalive_idle_secs"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_keepalive_interval_secs"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_keepalive_retries"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_host_selection"
python = true
typescript = true
cpp = true

[[class]]
name = "FpssConfig.fpss_host_shuffle_seed"
python = true
typescript = true
cpp = true

# `FpssConfig.flush_mode` is exposed on every binding:
# C ABI (`tdx_config_set_flush_mode`), C++ (`set_flush_mode`),
# Python (`Config.flush_mode` getter + setter accepting
# `"batched"` / `"immediate"`), TypeScript (`Config.flushMode`
# getter + `setFlushMode` accepting the same strings).
[[class]]
name = "FpssConfig.flush_mode"
python = true
typescript = true
cpp = true

# `FpssConfig.derive_ohlcvc` is exposed on the C ABI
# (`tdx_config_set_derive_ohlcvc`) + Python (`Config.derive_ohlcvc`)
# + C++ (`set_derive_ohlcvc`) but not on TypeScript. The row
# captures the partial-binding state.
[[class]]
name = "FpssConfig.derive_ohlcvc"
python = true
typescript = false
cpp = true

# ─── AuthConfig / MetricsConfig cross-binding setters ──────────────────
#
# Operator-tuning knobs on `DirectConfig.auth` + `DirectConfig.metrics`.
# Bound on every binding: Python via `Config.nexus_url` /
# `Config.client_type` / `Config.metrics_port` setters/getters,
# TypeScript via `Config.setNexusUrl` / `setClientType` /
# `setMetricsPort`, C++ via `tdx::Config::set_nexus_url` /
# `set_client_type` / `set_metrics_port` (which forward to the FFI
# `tdx_config_set_nexus_url` / `_client_type` / `_metrics_port`
# symbols). The two `AuthConfig` fields are `String` — the FFI
# setter takes a `*const c_char` (validated UTF-8) and the getter
# returns a heap-owned `*mut c_char` released via `tdx_string_free`.
# `MetricsConfig.port` is `Option<u16>`, carried across the C ABI as
# the widened `(has_value, port)` shape so the disabled-exporter
# `None` sentinel survives the boundary.

[[class]]
name = "AuthConfig.nexus_url"
python = true
typescript = true
cpp = true

[[class]]
name = "AuthConfig.client_type"
python = true
typescript = true
cpp = true

[[class]]
name = "MetricsConfig.port"
python = true
typescript = true
cpp = true


# ─── Method-level parity matrix ────────────────────────────────────────
#
# `[[method]]` rows pin the per-method binding state on the load-bearing
# user-facing classes (`ThetaDataDxClient`, `FpssClient`, `Credentials`,
# `Config`). The class-level `[[class]]` rows above only record that
# the class exists in each binding; without these `[[method]]` rows a
# binding could quietly drop a public method (or fail to add a new one
# alongside its siblings) and no checker would notice. Each row carries:
#
#   class       — the canonical class name (Python pyclass / TS napi
#                 class / C++ class). For C++ alias names see
#                 `scripts/check_binding_parity.py::CPP_ALIASES`
#                 (`ThetaDataDxClient` -> `UnifiedClient`).
#   name        — the canonical method name in camelCase. The parity
#                 checker derives the snake_case form for Python +
#                 C++ matching and uses the camelCase form to match
#                 `#[napi(js_name = "...")]` declarations.
#   python      — `true` iff a `fn <snake_case>` declaration lives
#                 inside an `impl <class>` block on the Python source.
#   typescript  — `true` iff a `#[napi(js_name = "<camelCase>")]`
#                 attribute appears anywhere in `sdks/typescript/src/`.
#   cpp         — `true` iff a `<snake_case>(` member declaration
#                 appears inside the C++ class body in `thetadx.hpp`.
#
# Set a column to `false` to record a deliberate cross-binding
# asymmetry (e.g. the TS unified client deliberately exposes
# `setCallback` via the napi `startStreaming(callback)` factory rather
# than a separate method — the row captures that). Adding a method on
# one binding without flipping the matching column trips the gate;
# removing a method from a binding without updating the matrix also
# trips the gate.
#
# Scope: high-visibility user-facing classes only. Configuration
# setters (`Config.*`) remain governed by the dotted `[[class]]` rows
# above (e.g. `ReconnectConfig.wait_ms`) which already cover the
# cross-binding setter contract at the field level.

# ── ThetaDataDxClient (the unified entry-point client) ──

[[method]]
class = "ThetaDataDxClient"
name = "startStreaming"
python = true
typescript = true
cpp = false  # C++ uses `setCallback` for the same role; tracked below

[[method]]
class = "ThetaDataDxClient"
name = "stopStreaming"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "shutdown"
python = true
typescript = true
cpp = false  # C++ frees through the RAII destructor on `UnifiedClient`

[[method]]
class = "ThetaDataDxClient"
name = "reconnect"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "isStreaming"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "awaitDrain"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "subscribe"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "subscribeMany"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "unsubscribe"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "unsubscribeMany"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "activeSubscriptions"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "activeFullSubscriptions"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "droppedEventCount"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "panicCount"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "millisSinceLastEvent"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "lastEventReceivedAtUnixNanos"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "lastConnectedAddr"
python = true
typescript = true
cpp = true

[[method]]
class = "ThetaDataDxClient"
name = "setCallback"
python = false  # Python uses `startStreaming(callback)` for the same role
typescript = false  # TypeScript uses `startStreaming(callback)` likewise
cpp = true

# ── FpssClient (the standalone streaming-only client) ──
#
# Not on TypeScript today (tracked as a follow-up under issue #545 in
# the class-level row above). Every `typescript = false` below reflects
# the same gap and is NOT an action item by itself.

[[method]]
class = "FpssClient"
name = "startStreaming"
python = true
typescript = false
cpp = false  # C++ uses `setCallback` for the same role

[[method]]
class = "FpssClient"
name = "stopStreaming"
python = true
typescript = false
cpp = false  # C++ frees through the RAII destructor on `FpssClient`

[[method]]
class = "FpssClient"
name = "shutdown"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "reconnect"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "isStreaming"
python = true
typescript = false
cpp = false  # C++ `FpssClient` is always live once constructed

[[method]]
class = "FpssClient"
name = "isAuthenticated"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "awaitDrain"
python = true
typescript = false
cpp = false  # C++ drain barrier runs inside the RAII destructor

[[method]]
class = "FpssClient"
name = "subscribe"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "subscribeMany"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "unsubscribe"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "unsubscribeMany"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "activeSubscriptions"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "activeFullSubscriptions"
python = true
typescript = false
cpp = false  # C++ FpssClient surface omits the full-stream snapshot today

[[method]]
class = "FpssClient"
name = "droppedEventCount"
python = true
typescript = false
cpp = false  # C++ method is `dropped_events`, declared separately below

[[method]]
class = "FpssClient"
name = "droppedEvents"
python = false
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "panicCount"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "millisSinceLastEvent"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "lastEventReceivedAtUnixNanos"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "lastConnectedAddr"
python = true
typescript = false
cpp = true

[[method]]
class = "FpssClient"
name = "setCallback"
python = false
typescript = false
cpp = true

# ── Credentials (the auth-handle class) ──
#
# TypeScript takes credentials as constructor arguments rather than a
# class (see the class-level row above), so every column below is
# `false` — the row exists to document the cross-binding factory
# contract on Python + C++.

[[method]]
class = "Credentials"
name = "fromFile"
python = true
typescript = false
cpp = true

[[method]]
class = "Credentials"
name = "fromEmail"
python = false  # Python uses the `Credentials(email, password)` constructor
typescript = false
cpp = true

# ── Config (the streaming/historical client configuration class) ──
#
# Field-level setters are tracked by the dotted `[[class]]` rows above
# (e.g. `ReconnectConfig.wait_ms`, `RetryPolicy.initial_delay_ms`).
# The factory rows below pin the three environment-tier constructors.

[[method]]
class = "Config"
name = "production"
python = true
typescript = true
cpp = true

[[method]]
class = "Config"
name = "dev"
python = true
typescript = true
cpp = true

[[method]]
class = "Config"
name = "stage"
python = true
typescript = true
cpp = true

[[method]]
class = "Config"
name = "setFlushMode"
python = true
typescript = true
cpp = true

[[method]]
class = "Config"
name = "flushMode"
python = true
typescript = true
cpp = false  # follow-up — mirror getter in sdks/cpp/include/thetadx.hpp

# Custom reconnect-policy callback registration. A field-shaped getter
# is meaningless for a callback, so the registration is tracked as a
# method row (Python `config.reconnect_callback = fn` setter, TS
# `setReconnectCallback`, C++ `set_reconnect_callback` over the FFI
# `tdx_config_set_reconnect_callback` symbol).
[[method]]
class = "Config"
name = "setReconnectCallback"
python = true
typescript = true
cpp = true
