// @generated DO NOT EDIT — regenerated by build.rs from tick_schema.toml
// Columnar converters: JSON object where each key maps to an array.
// Returned as serde_json::Value for napi serialization.
//
// Opt-in path kept alongside the typed tick classes so DataFrame-style
// consumers can still get column arrays in one allocation. Each function
// is `#[allow(dead_code)]` because the default endpoint surface now
// returns typed `Vec<Tick>` instead.

#[allow(dead_code)]
fn calendar_days_to_columnar(ticks: &[tick::CalendarDay]) -> serde_json::Value {
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_is_open: Vec<i32> = ticks.iter().map(|t| t.is_open).collect();
    let col_open_time: Vec<i32> = ticks.iter().map(|t| t.open_time).collect();
    let col_close_time: Vec<i32> = ticks.iter().map(|t| t.close_time).collect();
    let col_status: Vec<i32> = ticks.iter().map(|t| t.status).collect();
    serde_json::json!({
        "date": col_date,
        "is_open": col_is_open,
        "open_time": col_open_time,
        "close_time": col_close_time,
        "status": col_status,
    })
}

#[allow(dead_code)]
fn eod_ticks_to_columnar(ticks: &[tick::EodTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_ms_of_day2: Vec<i32> = ticks.iter().map(|t| t.ms_of_day2).collect();
    let col_open: Vec<f64> = ticks.iter().map(|t| t.open).collect();
    let col_high: Vec<f64> = ticks.iter().map(|t| t.high).collect();
    let col_low: Vec<f64> = ticks.iter().map(|t| t.low).collect();
    let col_close: Vec<f64> = ticks.iter().map(|t| t.close).collect();
    let col_volume: Vec<i32> = ticks.iter().map(|t| t.volume).collect();
    let col_count: Vec<i32> = ticks.iter().map(|t| t.count).collect();
    let col_bid_size: Vec<i32> = ticks.iter().map(|t| t.bid_size).collect();
    let col_bid_exchange: Vec<i32> = ticks.iter().map(|t| t.bid_exchange).collect();
    let col_bid: Vec<f64> = ticks.iter().map(|t| t.bid).collect();
    let col_bid_condition: Vec<i32> = ticks.iter().map(|t| t.bid_condition).collect();
    let col_ask_size: Vec<i32> = ticks.iter().map(|t| t.ask_size).collect();
    let col_ask_exchange: Vec<i32> = ticks.iter().map(|t| t.ask_exchange).collect();
    let col_ask: Vec<f64> = ticks.iter().map(|t| t.ask).collect();
    let col_ask_condition: Vec<i32> = ticks.iter().map(|t| t.ask_condition).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "ms_of_day2": col_ms_of_day2,
        "open": col_open,
        "high": col_high,
        "low": col_low,
        "close": col_close,
        "volume": col_volume,
        "count": col_count,
        "bid_size": col_bid_size,
        "bid_exchange": col_bid_exchange,
        "bid": col_bid,
        "bid_condition": col_bid_condition,
        "ask_size": col_ask_size,
        "ask_exchange": col_ask_exchange,
        "ask": col_ask,
        "ask_condition": col_ask_condition,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn greeks_ticks_to_columnar(ticks: &[tick::GreeksTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_implied_volatility: Vec<f64> = ticks.iter().map(|t| t.implied_volatility).collect();
    let col_delta: Vec<f64> = ticks.iter().map(|t| t.delta).collect();
    let col_gamma: Vec<f64> = ticks.iter().map(|t| t.gamma).collect();
    let col_theta: Vec<f64> = ticks.iter().map(|t| t.theta).collect();
    let col_vega: Vec<f64> = ticks.iter().map(|t| t.vega).collect();
    let col_rho: Vec<f64> = ticks.iter().map(|t| t.rho).collect();
    let col_iv_error: Vec<f64> = ticks.iter().map(|t| t.iv_error).collect();
    let col_vanna: Vec<f64> = ticks.iter().map(|t| t.vanna).collect();
    let col_charm: Vec<f64> = ticks.iter().map(|t| t.charm).collect();
    let col_vomma: Vec<f64> = ticks.iter().map(|t| t.vomma).collect();
    let col_veta: Vec<f64> = ticks.iter().map(|t| t.veta).collect();
    let col_speed: Vec<f64> = ticks.iter().map(|t| t.speed).collect();
    let col_zomma: Vec<f64> = ticks.iter().map(|t| t.zomma).collect();
    let col_color: Vec<f64> = ticks.iter().map(|t| t.color).collect();
    let col_ultima: Vec<f64> = ticks.iter().map(|t| t.ultima).collect();
    let col_d1: Vec<f64> = ticks.iter().map(|t| t.d1).collect();
    let col_d2: Vec<f64> = ticks.iter().map(|t| t.d2).collect();
    let col_dual_delta: Vec<f64> = ticks.iter().map(|t| t.dual_delta).collect();
    let col_dual_gamma: Vec<f64> = ticks.iter().map(|t| t.dual_gamma).collect();
    let col_epsilon: Vec<f64> = ticks.iter().map(|t| t.epsilon).collect();
    let col_lambda: Vec<f64> = ticks.iter().map(|t| t.lambda).collect();
    let col_vera: Vec<f64> = ticks.iter().map(|t| t.vera).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "implied_volatility": col_implied_volatility,
        "delta": col_delta,
        "gamma": col_gamma,
        "theta": col_theta,
        "vega": col_vega,
        "rho": col_rho,
        "iv_error": col_iv_error,
        "vanna": col_vanna,
        "charm": col_charm,
        "vomma": col_vomma,
        "veta": col_veta,
        "speed": col_speed,
        "zomma": col_zomma,
        "color": col_color,
        "ultima": col_ultima,
        "d1": col_d1,
        "d2": col_d2,
        "dual_delta": col_dual_delta,
        "dual_gamma": col_dual_gamma,
        "epsilon": col_epsilon,
        "lambda": col_lambda,
        "vera": col_vera,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn interest_rate_ticks_to_columnar(ticks: &[tick::InterestRateTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_rate: Vec<f64> = ticks.iter().map(|t| t.rate).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "rate": col_rate,
        "date": col_date,
    })
}

#[allow(dead_code)]
fn iv_ticks_to_columnar(ticks: &[tick::IvTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_implied_volatility: Vec<f64> = ticks.iter().map(|t| t.implied_volatility).collect();
    let col_iv_error: Vec<f64> = ticks.iter().map(|t| t.iv_error).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "implied_volatility": col_implied_volatility,
        "iv_error": col_iv_error,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn market_value_ticks_to_columnar(ticks: &[tick::MarketValueTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_market_bid: Vec<f64> = ticks.iter().map(|t| t.market_bid).collect();
    let col_market_ask: Vec<f64> = ticks.iter().map(|t| t.market_ask).collect();
    let col_market_price: Vec<f64> = ticks.iter().map(|t| t.market_price).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "market_bid": col_market_bid,
        "market_ask": col_market_ask,
        "market_price": col_market_price,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn ohlc_ticks_to_columnar(ticks: &[tick::OhlcTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_open: Vec<f64> = ticks.iter().map(|t| t.open).collect();
    let col_high: Vec<f64> = ticks.iter().map(|t| t.high).collect();
    let col_low: Vec<f64> = ticks.iter().map(|t| t.low).collect();
    let col_close: Vec<f64> = ticks.iter().map(|t| t.close).collect();
    let col_volume: Vec<i32> = ticks.iter().map(|t| t.volume).collect();
    let col_count: Vec<i32> = ticks.iter().map(|t| t.count).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "open": col_open,
        "high": col_high,
        "low": col_low,
        "close": col_close,
        "volume": col_volume,
        "count": col_count,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn open_interest_ticks_to_columnar(ticks: &[tick::OpenInterestTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_open_interest: Vec<i32> = ticks.iter().map(|t| t.open_interest).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "open_interest": col_open_interest,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn option_contracts_to_columnar(ticks: &[tick::OptionContract]) -> serde_json::Value {
    let col_root: Vec<&str> = ticks.iter().map(|t| t.root.as_str()).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<i32> = ticks.iter().map(|t| t.right).collect();
    serde_json::json!({
        "root": col_root,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn price_ticks_to_columnar(ticks: &[tick::PriceTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_price: Vec<f64> = ticks.iter().map(|t| t.price).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "price": col_price,
        "date": col_date,
    })
}

#[allow(dead_code)]
fn quote_ticks_to_columnar(ticks: &[tick::QuoteTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_bid_size: Vec<i32> = ticks.iter().map(|t| t.bid_size).collect();
    let col_bid_exchange: Vec<i32> = ticks.iter().map(|t| t.bid_exchange).collect();
    let col_bid: Vec<f64> = ticks.iter().map(|t| t.bid).collect();
    let col_bid_condition: Vec<i32> = ticks.iter().map(|t| t.bid_condition).collect();
    let col_ask_size: Vec<i32> = ticks.iter().map(|t| t.ask_size).collect();
    let col_ask_exchange: Vec<i32> = ticks.iter().map(|t| t.ask_exchange).collect();
    let col_ask: Vec<f64> = ticks.iter().map(|t| t.ask).collect();
    let col_ask_condition: Vec<i32> = ticks.iter().map(|t| t.ask_condition).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_midpoint: Vec<f64> = ticks.iter().map(|t| t.midpoint).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "bid_size": col_bid_size,
        "bid_exchange": col_bid_exchange,
        "bid": col_bid,
        "bid_condition": col_bid_condition,
        "ask_size": col_ask_size,
        "ask_exchange": col_ask_exchange,
        "ask": col_ask,
        "ask_condition": col_ask_condition,
        "date": col_date,
        "midpoint": col_midpoint,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn trade_quote_ticks_to_columnar(ticks: &[tick::TradeQuoteTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_sequence: Vec<i32> = ticks.iter().map(|t| t.sequence).collect();
    let col_ext_condition1: Vec<i32> = ticks.iter().map(|t| t.ext_condition1).collect();
    let col_ext_condition2: Vec<i32> = ticks.iter().map(|t| t.ext_condition2).collect();
    let col_ext_condition3: Vec<i32> = ticks.iter().map(|t| t.ext_condition3).collect();
    let col_ext_condition4: Vec<i32> = ticks.iter().map(|t| t.ext_condition4).collect();
    let col_condition: Vec<i32> = ticks.iter().map(|t| t.condition).collect();
    let col_size: Vec<i32> = ticks.iter().map(|t| t.size).collect();
    let col_exchange: Vec<i32> = ticks.iter().map(|t| t.exchange).collect();
    let col_price: Vec<f64> = ticks.iter().map(|t| t.price).collect();
    let col_condition_flags: Vec<i32> = ticks.iter().map(|t| t.condition_flags).collect();
    let col_price_flags: Vec<i32> = ticks.iter().map(|t| t.price_flags).collect();
    let col_volume_type: Vec<i32> = ticks.iter().map(|t| t.volume_type).collect();
    let col_records_back: Vec<i32> = ticks.iter().map(|t| t.records_back).collect();
    let col_quote_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.quote_ms_of_day).collect();
    let col_bid_size: Vec<i32> = ticks.iter().map(|t| t.bid_size).collect();
    let col_bid_exchange: Vec<i32> = ticks.iter().map(|t| t.bid_exchange).collect();
    let col_bid: Vec<f64> = ticks.iter().map(|t| t.bid).collect();
    let col_bid_condition: Vec<i32> = ticks.iter().map(|t| t.bid_condition).collect();
    let col_ask_size: Vec<i32> = ticks.iter().map(|t| t.ask_size).collect();
    let col_ask_exchange: Vec<i32> = ticks.iter().map(|t| t.ask_exchange).collect();
    let col_ask: Vec<f64> = ticks.iter().map(|t| t.ask).collect();
    let col_ask_condition: Vec<i32> = ticks.iter().map(|t| t.ask_condition).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "sequence": col_sequence,
        "ext_condition1": col_ext_condition1,
        "ext_condition2": col_ext_condition2,
        "ext_condition3": col_ext_condition3,
        "ext_condition4": col_ext_condition4,
        "condition": col_condition,
        "size": col_size,
        "exchange": col_exchange,
        "price": col_price,
        "condition_flags": col_condition_flags,
        "price_flags": col_price_flags,
        "volume_type": col_volume_type,
        "records_back": col_records_back,
        "quote_ms_of_day": col_quote_ms_of_day,
        "bid_size": col_bid_size,
        "bid_exchange": col_bid_exchange,
        "bid": col_bid,
        "bid_condition": col_bid_condition,
        "ask_size": col_ask_size,
        "ask_exchange": col_ask_exchange,
        "ask": col_ask,
        "ask_condition": col_ask_condition,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

#[allow(dead_code)]
fn trade_ticks_to_columnar(ticks: &[tick::TradeTick]) -> serde_json::Value {
    let col_ms_of_day: Vec<i32> = ticks.iter().map(|t| t.ms_of_day).collect();
    let col_sequence: Vec<i32> = ticks.iter().map(|t| t.sequence).collect();
    let col_ext_condition1: Vec<i32> = ticks.iter().map(|t| t.ext_condition1).collect();
    let col_ext_condition2: Vec<i32> = ticks.iter().map(|t| t.ext_condition2).collect();
    let col_ext_condition3: Vec<i32> = ticks.iter().map(|t| t.ext_condition3).collect();
    let col_ext_condition4: Vec<i32> = ticks.iter().map(|t| t.ext_condition4).collect();
    let col_condition: Vec<i32> = ticks.iter().map(|t| t.condition).collect();
    let col_size: Vec<i32> = ticks.iter().map(|t| t.size).collect();
    let col_exchange: Vec<i32> = ticks.iter().map(|t| t.exchange).collect();
    let col_price: Vec<f64> = ticks.iter().map(|t| t.price).collect();
    let col_condition_flags: Vec<i32> = ticks.iter().map(|t| t.condition_flags).collect();
    let col_price_flags: Vec<i32> = ticks.iter().map(|t| t.price_flags).collect();
    let col_volume_type: Vec<i32> = ticks.iter().map(|t| t.volume_type).collect();
    let col_records_back: Vec<i32> = ticks.iter().map(|t| t.records_back).collect();
    let col_date: Vec<i32> = ticks.iter().map(|t| t.date).collect();
    let col_expiration: Vec<i32> = ticks.iter().map(|t| t.expiration).collect();
    let col_strike: Vec<f64> = ticks.iter().map(|t| t.strike).collect();
    let col_right: Vec<&str> = ticks.iter().map(|t| if t.is_call() { "C" } else if t.is_put() { "P" } else { "" }).collect();
    serde_json::json!({
        "ms_of_day": col_ms_of_day,
        "sequence": col_sequence,
        "ext_condition1": col_ext_condition1,
        "ext_condition2": col_ext_condition2,
        "ext_condition3": col_ext_condition3,
        "ext_condition4": col_ext_condition4,
        "condition": col_condition,
        "size": col_size,
        "exchange": col_exchange,
        "price": col_price,
        "condition_flags": col_condition_flags,
        "price_flags": col_price_flags,
        "volume_type": col_volume_type,
        "records_back": col_records_back,
        "date": col_date,
        "expiration": col_expiration,
        "strike": col_strike,
        "right": col_right,
    })
}

